# Best practice: pin versions (==) so others get the same environment.
# Use loose constraints (>=, ~=) only when you deliberately want to allow compatibility.

# Web / API
requests==2.32.3
httpx==0.27.2
fastapi==0.115.0
uvicorn[standard]==0.31.0

# Data
pandas==2.2.3
numpy==2.1.1

# AI / LLM
openai==1.51.0
azure-identity==1.18.0
azure-ai-projects==1.0.0b3

# Utilities
python-dotenv==1.0.1
pydantic==2.9.2

# Dev (optional)
# pytest==8.3.3
# ruff==0.6.9
